<Window x:Class="BeamNG.IDE.StartPage.StartPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeamNG.IDE.StartPage"
        mc:Ignorable="d"
        Title="Projects" Height="450" Width="350" Loaded="Window_Loaded" Background="{x:Null}" ResizeMode="CanMinimize">
    <Grid Margin="0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>
        <ListView Name="recentList" Grid.Column="1" Grid.Row="1" Grid.RowSpan="1" Margin="0,0" MouseDoubleClick="recentList_MouseDoubleClick" Background="{x:Null}" BorderBrush="{x:Null}" IsTextSearchEnabled="False" HorizontalContentAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Hidden">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <WrapPanel>
                        <Image Width="300" Height="60" Source="{Binding bitmap}" />
                    </WrapPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
